import math
numero = int(input("Introduce un número: "))
es_primo = True
if numero < 2:
    es_primo = False
else:
    for i in range(2, int(math.sqrt(numero)) + 1):
        if numero % i == 0:
            es_primo = False
            break
if es_primo:
    print("Es primo")
else:
    print("No es primo")