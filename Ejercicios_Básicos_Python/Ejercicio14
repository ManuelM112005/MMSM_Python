# Leer una cadena y contar cuántos caracteres numéricos ('0' a '9') contiene.

cadena = "Python 3.10 released in 2021"
contador_numeros = 0

for caracter in cadena:
    if '0' <= caracter <= '9':
        contador_numeros += 1

print(contador_numeros)